import 'package:example/naming.dart';

const errorRequired = 'required';
const errorMustMatch = 'Must match validator demo';

const email = Naming('Email');
const password = Naming('Password');

const submitRaw = Naming('Submit raw');
const submit = Naming('Submit');
const updateModel = Naming('Update model');
const reset = Naming('Reset');
